user:
  name: unknown
  github: unknown

language: "python"
python_version: "3.9.17"

commands:
  build:
    - "pip install -r requirements.txt"
  package:
    - "libffi-dev libjpeg-dev musl php-pgsql python3-lxml postgresql postgresql-client python3-psycopg2 libpq-dev libcurl4-openssl-dev libxml2-dev libxslt1-dev python3-pip python3-requests python3-sqlalchemy python3-tz python3-aiohttp openssl pv jq python3 python3-dev libreadline-dev libyaml-dev gcc sqlite3 libsqlite3-dev sudo zlib1g ffmpeg libssl-dev libgconf-2-4 libxi6 libopus0 libopus-dev nano"
  start:
    - "python3 bot.py"

env:
  - "ENVIRONMENT=production"
