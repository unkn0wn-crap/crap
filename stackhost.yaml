user:
  name: unknown
  github: unknown

language: "python"
python_version: "3.9.17"

commands:
  build:
    - "pip install -r requirements.txt"
  package:
    - "nano ffmpeg"
  start:
    - "python3 bot.py"

env:
  - "ENVIRONMENT=production"
