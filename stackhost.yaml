user:
  name: unknown
  github: unknown

language: "python"
python_version: "3.10.8"

commands:
  build:
    - "pip install -r requirements.txt"
  package:
    - "ffmpeg nano neofetch"
  start:
    - "python3 bot.py"

env:
  - "ENVIRONMENT=production"
