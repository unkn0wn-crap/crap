user:
  name: unknown
  github: unknown

language: "python"
python_version: "3.9.17"

commands:
  build:
    - "pip install -r requirements.txt"
  package:
    - "nano ffmpeg postgresql postgresql-client python3-psycopg2 python3-pip python3-requests python3-sqlalchemy python3-tz python3-aiohttp openssl pv jq python3 python3-dev"
  start:
    - "python3 bot.py"

env:
  - "ENVIRONMENT=production"
